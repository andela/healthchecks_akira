machine:
    python:
        version: 3.5.2
dependencies:
    pre:
        - pip install -r requirements.txt
        - pip install mock
test:
    override:
        - python manage.py migrate
        - python manage.py test
